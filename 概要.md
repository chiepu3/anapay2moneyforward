# ANAPay2MoneyForward 改
このドキュメントは、ANAPay2MoneyForwardを私の使用状況に合わせて改変するために必要な情報をまとめたものです。

## 目的
MoneyForwardは、個人の資産管理や家計簿の作成に役立つサービスですが、私がよく使うANA Pay、JAL Pay、楽天ペイとの連携が行えません。そこで、各サービスを利用した際に届く利用内容のメールから、必要な情報を抽出し、MoneyForwardに取り込む仕組みを作成します。
MoneyForwardのAPIを利用したいところですが残念ながら存在しないため、都度UI操作して情報を反映する必要があります。

## 概要
私は元のサービスに対して、すでに以下の変更を行っています。

1. メインに使っていた住信SBIネット銀行のデビットカードだと利用店名が表示されず、「デビット xxxxxx」としか表示されないため、利用店名を表示できるよう、メールから件名を取得し、使った額分の情報を新しく登録しようとしていた（実際は、デビット xxxxxxの情報を完全に削除することができないため、この方法は一時断念。現在は住信SBIネット銀行のデビットカードは一時的に使用していないため、今は問題なし）。
2. ANA Payのみ実装。元のプロジェクトと同じように、GMailからANA Payの利用内容のメールを取得し、必要な情報を抽出してMoneyForwardに登録する。
3. MoneyForwardについても、もとは他の支店のシステムを使うようになっていたため、本家MoneyForwardのシステムを使うように変更。その際に都度メール認証が必要になってしまったが、仕方ない。

## 行ってほしいこと

1. ファイルの分割を行い、可読性の向上を行う。
2. ANA Pay、JAL Payの実装（可能ならデビットカードともまとめ、汎用化）
入金、出金の記録を把握し、それぞれにどれだけの資産があるかを把握できるようにする。
3. 楽天ペイの実装(これは若干複雑)
4. カテゴリの設定を完全に自動化したい。そのために、起動ルールなどに大きな変更が必要である
5. 金融機関が存在しない場合は、口座の作成を行う。
6. ログイン情報が保存されておらず、都度ログインを求められるので、その対策

## 詳細

### 口座の作成
1. https://moneyforward.com/accounts/new/wallet
2. 以下要素に口座名を指定
```HTML
<input placeholder="（例）財布" type="text" name="new_wallet_account_info[name]" id="new_wallet_account_info_name">
```

3. 以下要素で金額を指定
```HTML
<input placeholder="（例）21000" type="text" name="new_wallet_account_info[value]" id="new_wallet_account_info_value">
```
3. そのままだと、一部銀行から引き出した際に勝手に「財布」口座の残額が増えるため、ATM引き出しとの連携を解除
#js-select-nothingをクリック
4. 「登録」ボタンをクリック
```HTML
<input type="submit" name="commit" value="登録" class="btn btn-success" data-disable-with="登録">
```

4. Alertが出てくるので、閉じる

### 各サービスの管理
基本的には今と同じように、メールを監視し、必要な情報を抽出してMoneyForwardに登録する。
しかし、一部に注意点がある。それは振替の設定。

現在、ユーザーは主にこのようにしてANA Pay、JAL Pay、楽天ペイを利用している。
1. 各種クレジットカード -> JAL Payにチャージ
2. JAL Pay -> ANA Payにチャージ
3. ANA Pay -> 楽天Edyにチャージ
4. 楽天Edy -> 楽天ペイ(楽天キャッシュ)にチャージ
5. 楽天ペイで支払い

このうち、MoneyForwardで公式に対応しているのは、1と3、4のみである。
しかしそれらも完ぺきではない。例えば、各種クレジットカードからJAL Payにチャージした際、本質的にはクレジットカードからJAL Payへ振替したことになるが、MoneyForwardでは単なる負債として扱われる。
クレジットカードからJAL Payへのチャージを「負債」としてしまうのは簡単だが、そうなると家計簿管理としては非常に分かりづらくなる。
両方ともこのシステムで管理するJAL PayからANA Payへのチャージは比較的簡単だが、クレジットカードからJAL Payへのチャージは、MoneyForwardで公式に記録が残されてから、負債として扱われることを避けるために、「計算対象から外す」設定を行う必要がある。その代わりに、このシステムで手動（？）で、JAL Payへ入金する必要がある。

また3のANAPayから楽天Edyのチャージについては、MoneyForward側でも「振替」として扱っているものの、その振替元が指定されていない状態になっているため、全体の資産は増えたように見えてしまう。これをメールの情報（金額、日時、使用した口座）を参照し、適切に振替元をUI上で設定してあげる必要がある。

4.の楽天Edyから楽天ペイへのチャージについては記録自体はされているものの、「電子マネーの支出」として扱われてしまうため、これも適切ではない。具体的には、月の支出欄で「電子マネー」として表示されるだけのため、楽天ペイでの支出がどれだけあったかを把握することができない。そのため、これも楽天ペイへの振り替えとして記録する必要がある。


### カテゴリ設定の完全自動化
やってほしいこと
- MoneyForwardの登録は後にしておいて、常時メールを監視する
- 利用確認メールが届いた際、LLMを用いるか、ユーザーに通知を飛ばすことで、その店のカテゴリを設定する
- 通知はDiscordなどで行う。LLMがカテゴリを推察し、いくつかの例を提示する。ユーザーはそれに対して回答する。
- その情報もGoogleスプレッドシートに保存しておく。以降同じ店からの利用はその情報をもとにカテゴリを設定する。
- これを行おうとすることにより、今とはかなり異なる形態にする必要がある（常時起動など）。
- 